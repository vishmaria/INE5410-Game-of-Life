-Criar as threads antes de inicias as gerações
    -A cada geração é criada n_threads novas e esperado sua sincronzação

-Cada thread tera uma coordenada da matriz para o calculo(lógica da af de mutex)
    -Lógica para definir qual coordenada cada thread fara no momento(mutex para setar as coordenadas(mutex para x e y))
    -Escrita na estatistica da geração(mutex para cada estatistica)

-Escrita nas estatisticas totais não precisará de mutex